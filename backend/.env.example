# ── Application ─────────────────────────────────────────────
APP_NAME=GYM SYLLA API
APP_ENV=development
API_V1_PREFIX=/api/v1

# ── Base de données ─────────────────────────────────────────
# Local (Docker) :
DATABASE_URL=postgresql://gym_user:gym_password@localhost:5432/gym_sylla
#
# Supabase (prod) — remplacer par votre connection string :
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ── Sécurité JWT ────────────────────────────────────────────
SECRET_KEY=changez-moi-en-production-avec-une-cle-longue-et-aleatoire
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── Métier ──────────────────────────────────────────────────
DEVISE=MRU
TARIF_SEANCE_JOURNALIERE=50

# ── CORS ────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:4200

# ── Notifications ───────────────────────────────────────────
NOTIFICATION_SERVICE_URL=http://localhost:3001
NOTIFICATION_API_SECRET=total_fitness_secret_2026

# Port du micro-service
PORT=3001

# Clé partagée avec le backend FastAPI (sécurise les appels)
API_SECRET=sb_secret_01Wv-G70sgHX5H0W3W-tbA_pNuO1LPt

# Provider SMS de secours (optionnel)
SMS_PROVIDER=
SMS_API_KEY=
SMS_SENDER=TOTALFIT