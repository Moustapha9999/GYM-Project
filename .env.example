# Variables Docker Compose — Machine A (serveur PostgreSQL)
# Copier vers .env à la racine : cp .env.example .env

POSTGRES_USER=gym_user
POSTGRES_PASSWORD=gym_password_dev_only
POSTGRES_DB=gym_sylla

# pgAdmin (optionnel)
PGADMIN_EMAIL=admin@totalfitness.mr
PGADMIN_PASSWORD=changeme_pgadmin
