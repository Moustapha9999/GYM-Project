# Frontend — Angular 20

Interface utilisateur du projet GYM SYLLA.

## Installation
```bash
npm install
ng serve
```

Configurer l'URL de l'API dans `src/environments/environment.ts`.
Voir le [contrat d'API](../docs/api-contract.md).
